{
  "name": "@dnerolabs/wallet-metadata",
  "version": "1.0.24",
  "description": "A mapping of DNC20 contract addresses to broadly accepted icons for those tokens.",
  "main": "index.js",
  "devDependencies": {
    "npm-version": "~1.1.0"
  },
  "scripts": {
    "bump-version": "yarn version --patch --no-git-tag-version",
    "build": "node build.js",
    "release:prod": "./bin/release-prod.sh"
  },
  "author": "Dnero Labs",
  "license": "ISC"
}
